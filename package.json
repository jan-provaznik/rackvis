{
  "dependencies": {
    "bluebird": "^3.7.2",
    "classnames": "^2.3.1",
    "d3": "^7.4.4",
    "d3-svg-to-png": "^0.1.7"
  },
  "scripts": {
    "client:serve": "parcel source/index.html --no-autoinstall --dist-dir .build/runtime",
    "client:build": "parcel build source/index.html --no-autoinstall --dist-dir build --public-url ./"
  },
  "browserslist": [
    "last 5 Chrome version"
  ],
  "alias": {
    "vis": "./source/es6"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "parcel": "^2.5.0"
  }
}
